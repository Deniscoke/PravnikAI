/** @type {import('next').NextConfig} */
const nextConfig = {
  // Keep existing static files accessible during transition
  // (index.html, index-cz.html won't conflict with Next.js pages)
}

module.exports = nextConfig
